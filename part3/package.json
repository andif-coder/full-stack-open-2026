{
  "name": "part3",
  "version": "1.0.0",
  "description": "backend for phonebook",
  "license": "ISC",
  "author": "cwj",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "deploy": "rm -rf dist && cd ../part2/phonebook && npm run build && cp -r dist ../../part3"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.1",
    "morgan": "^1.11.0"
  }
}
